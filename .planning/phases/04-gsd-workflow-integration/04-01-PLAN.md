---
phase: 04-gsd-workflow-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/new-project.md
  - .claude/commands/gsd/new-project.md
autonomous: true
requirements: [R1.3, R4.1]

must_haves:
  truths:
    - "Design thinking is offered to the user after PROJECT.md is written during /gsd:new-project"
    - "Skipping design thinking results in no DESIGN.md and vanilla GSD behavior continues"
    - "Auto mode skips design thinking entirely without blocking"
    - "If DESIGN.md already exists, design thinking step is skipped automatically"
  artifacts:
    - path: "workflows/new-project.md"
      provides: "Patched new-project workflow with design thinking injection"
      contains: "GSD-DESIGN-START"
    - path: ".claude/commands/gsd/new-project.md"
      provides: "Updated command shim with design-thinking execution_context reference"
      contains: "design-thinking"
  key_links:
    - from: "workflows/new-project.md"
      to: ".claude/commands/gsd/design-thinking.md"
      via: "@ reference in injection block"
      pattern: "design-thinking"
    - from: "workflows/new-project.md"
      to: ".planning/DESIGN.md"
      via: "guard clause checking existence"
      pattern: "DESIGN\\.md"
---

<objective>
Patch the new-project workflow to inject a design thinking step between Step 4 (Write PROJECT.md) and Step 5 (Workflow Preferences). Update the command shim to include the design-thinking command in its execution_context.

Purpose: Wire the design thinking interview (Phase 1) into GSD's project initialization so users are offered design thinking at the right moment -- after PROJECT.md exists but before research begins.

Output: Patched `workflows/new-project.md` and `.claude/commands/gsd/new-project.md`
</objective>

<execution_context>
@/Users/jayvanam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jayvanam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-gsd-workflow-integration/04-RESEARCH.md

Source files to read before patching:
@~/.claude/get-shit-done/workflows/new-project.md
@.claude/commands/gsd/new-project.md
@.claude/commands/gsd/design-thinking.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject design thinking step into new-project workflow</name>
  <files>workflows/new-project.md</files>
  <action>
1. Copy `~/.claude/get-shit-done/workflows/new-project.md` to `workflows/new-project.md` in the project repo (this is the forked copy that the installer will overlay).

2. Find the boundary between Step 4 (Write PROJECT.md -- ends with the `node gsd-tools.cjs commit` command around line 343) and Step 5 (Workflow Preferences -- starts with `## 5. Workflow Preferences` around line 346).

3. Insert a new section between them, wrapped in marker comments. The injection block must:

```markdown
<!-- GSD-DESIGN-START -->
## 4.5. Design Thinking (optional)

**If auto mode:** Skip design thinking entirely. Log: "Design thinking skipped (auto mode)." Proceed to Step 5.

**If interactive mode:**

Check if `.planning/DESIGN.md` already exists:
- If yes: Log "DESIGN.md already exists -- skipping design thinking." Proceed to Step 5.
- If no: Offer design thinking to the user.

```
AskUserQuestion([
  {
    question: "Would you like to run design thinking? This creates a design brief (DESIGN.md) that guides UI decisions in later phases.",
    header: "Design Thinking",
    multiSelect: false,
    options: [
      { label: "Yes", description: "Run design thinking interview (5-10 minutes)" },
      { label: "Skip", description: "Continue without design -- vanilla GSD behavior" }
    ]
  }
])
```

**If "Skip":** Log "Design thinking skipped. No DESIGN.md will be created -- all downstream behavior is vanilla GSD." Proceed to Step 5.

**If "Yes":**
Read and execute the design thinking interview process from the design-thinking command:

@{path-to-project}/.claude/commands/gsd/design-thinking.md

Execute Steps 2-7 of the design-thinking process (skip Step 0 re-run check and Step 1 skip offer -- user just opted in). Specifically:
- Step 2: Load PROJECT.md for context
- Step 3: Problem Space interview
- Step 4: Emotional Core interview
- Step 5: Solution Space + Brand Identity interview
- Step 6: Generate DESIGN.md
- Step 7: Validation loop (user approves, edits, or regenerates)

After DESIGN.md is written and approved, commit it:
```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs: create DESIGN.md" --files .planning/DESIGN.md
```

Proceed to Step 5.
<!-- GSD-DESIGN-END -->
```

4. IMPORTANT: The `@{path-to-project}` reference should use the actual project-relative path: `@.claude/commands/gsd/design-thinking.md`. This tells Claude to load the design-thinking command file into context so it can execute the interview.

5. Do NOT modify any other part of the workflow file. The injection is purely additive between markers.
  </action>
  <verify>
    <automated>grep -c "GSD-DESIGN-START" workflows/new-project.md && grep -c "GSD-DESIGN-END" workflows/new-project.md && grep "DESIGN.md" workflows/new-project.md | grep -c "exists" && grep -c "auto mode" workflows/new-project.md</automated>
  </verify>
  <done>workflows/new-project.md contains one GSD-DESIGN-START/END marker pair between Step 4 and Step 5, with guard clause (DESIGN.md exists check), auto mode skip, AskUserQuestion for interactive mode, and @ reference to design-thinking command</done>
</task>

<task type="auto">
  <name>Task 2: Update new-project command shim execution_context</name>
  <files>.claude/commands/gsd/new-project.md</files>
  <action>
1. Copy `~/.claude/commands/gsd/new-project.md` to `.claude/commands/gsd/new-project.md` in the project repo (forked copy for installer overlay).

2. In the `<execution_context>` section, add a reference to the design-thinking command file so Claude can resolve the @ reference in the workflow injection:

```
@.claude/commands/gsd/design-thinking.md
```

Add this line AFTER the existing execution_context entries, inside the `<execution_context>` tags. Wrap in markers:

```markdown
<!-- GSD-DESIGN-START -->
@.claude/commands/gsd/design-thinking.md
<!-- GSD-DESIGN-END -->
```

3. Also update the `<execution_context>` to reference the forked workflow file instead of the global one. Change:
```
@/Users/jayvanam/.claude/get-shit-done/workflows/new-project.md
```
to reference the forked version. The simplest approach: add a comment noting the forked workflow replaces the original. The installer handles the actual file replacement, so the path stays the same but the file content is the patched version.

NOTE: Actually, do NOT change the workflow path reference. The installer copies the patched workflow to the global location, so the existing path remains correct. Only add the design-thinking command reference.

4. Do NOT modify anything else in the command shim.
  </action>
  <verify>
    <automated>grep "design-thinking" .claude/commands/gsd/new-project.md && grep -c "GSD-DESIGN-START" .claude/commands/gsd/new-project.md</automated>
  </verify>
  <done>.claude/commands/gsd/new-project.md contains design-thinking.md reference in execution_context within GSD-DESIGN markers</done>
</task>

</tasks>

<verification>
All checks pass:
- `grep -c "GSD-DESIGN-START" workflows/new-project.md` returns 1+
- `grep -c "GSD-DESIGN-END" workflows/new-project.md` returns 1+
- `grep "design-thinking" workflows/new-project.md` shows @ reference to design-thinking command
- `grep "DESIGN.md" workflows/new-project.md` shows guard clause check
- `grep "auto mode" workflows/new-project.md` shows auto mode skip logic
- `grep "design-thinking" .claude/commands/gsd/new-project.md` shows execution_context reference
- The injection is located between "## 4. Write PROJECT.md" section end and "## 5. Workflow Preferences" section start
</verification>

<success_criteria>
- workflows/new-project.md exists as a complete copy of the GSD workflow with the design thinking injection added between Step 4 and Step 5
- The injection uses GSD-DESIGN-START/END markers
- Guard clause: DESIGN.md already exists = skip
- Auto mode: skip design thinking entirely
- Interactive mode: AskUserQuestion with Yes/Skip options
- "Yes" path references design-thinking command via @ reference
- .claude/commands/gsd/new-project.md includes design-thinking.md in execution_context
</success_criteria>

<output>
After completion, create `.planning/phases/04-gsd-workflow-integration/04-01-SUMMARY.md`
</output>
