---
phase: 06-update-safety-installation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/gsd/update.md
  - design-version.json
autonomous: true
requirements: [R6.1, R6.2]

must_haves:
  truths:
    - "Running /gsd:update preserves all design-layer files through the GSD clean wipe-and-replace cycle"
    - "design-version.json contains the fork version, install timestamp, GSD base version, and SHA-256 checksums for all 16 design-layer files"
  artifacts:
    - path: ".claude/commands/gsd/update.md"
      provides: "Patched update command that backs up and restores design files around GSD update"
      contains: "GSD-DESIGN"
    - path: "design-version.json"
      provides: "Version tracking schema template with file checksum map"
      contains: "sha256"
  key_links:
    - from: ".claude/commands/gsd/update.md"
      to: "design files in commands/gsd/ and get-shit-done/workflows/design/"
      via: "backup to temp dir before npx, restore after npx"
      pattern: "design-version\\.json|workflows/design"
---

<objective>
Create the update safety mechanism and version tracking for the design fork.

Purpose: GSD's update process performs a clean wipe-and-replace of `commands/gsd/` and `get-shit-done/` directories, which would destroy all design-layer files. This plan creates (1) a patched `update.md` command that backs up design files before the wipe and restores them after, and (2) a `design-version.json` schema for tracking fork version and file checksums.

Output: Patched `update.md` command shim, `design-version.json` template file
</objective>

<execution_context>
@/Users/jayvanam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jayvanam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-update-safety-installation/06-RESEARCH.md

<interfaces>
<!-- Vanilla GSD update.md command shim pattern (from ~/.claude/commands/gsd/update.md): -->
```markdown
---
name: gsd:update
description: Update GSD to latest version with changelog display
allowed-tools:
  - Bash
  - AskUserQuestion
---
<objective>...</objective>
<execution_context>
@/path/to/workflows/update.md
</execution_context>
<process>Follow the update workflow...</process>
```

<!-- Design-layer file inventory (16 files to preserve): -->
DESIGN-ONLY FILES (9):
  commands/gsd/design-thinking.md
  commands/gsd/design-ui.md
  commands/gsd/design-stack.md
  get-shit-done/workflows/design/motion-design.md
  get-shit-done/workflows/design/orchestrate-design.md
  get-shit-done/workflows/design/stack-conventions.md
  get-shit-done/workflows/design/ui-design.md
  get-shit-done/workflows/design/ui-detection.md
  get-shit-done/workflows/design/ux-design.md

PATCHED GSD FILES (6):
  commands/gsd/new-project.md
  commands/gsd/discuss-phase.md
  commands/gsd/plan-phase.md
  get-shit-done/workflows/new-project.md
  get-shit-done/workflows/discuss-phase.md
  get-shit-done/workflows/plan-phase.md

UPDATE COMMAND (1):
  commands/gsd/update.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create design-version.json schema template</name>
  <files>design-version.json</files>
  <action>
Create `design-version.json` at the project repo root. This is the template that installers will use as a reference and that the update.md command will preserve. The file tracks fork metadata and per-file SHA-256 checksums.

Structure:
```json
{
  "version": "1.0.0",
  "schema_version": 1,
  "installed_at": "",
  "gsd_base_version": "",
  "files": {
    "commands/gsd/design-thinking.md": "",
    "commands/gsd/design-ui.md": "",
    "commands/gsd/design-stack.md": "",
    "commands/gsd/new-project.md": "",
    "commands/gsd/discuss-phase.md": "",
    "commands/gsd/plan-phase.md": "",
    "commands/gsd/update.md": "",
    "get-shit-done/workflows/design/motion-design.md": "",
    "get-shit-done/workflows/design/orchestrate-design.md": "",
    "get-shit-done/workflows/design/stack-conventions.md": "",
    "get-shit-done/workflows/design/ui-design.md": "",
    "get-shit-done/workflows/design/ui-detection.md": "",
    "get-shit-done/workflows/design/ux-design.md": "",
    "get-shit-done/workflows/new-project.md": "",
    "get-shit-done/workflows/discuss-phase.md": "",
    "get-shit-done/workflows/plan-phase.md": ""
  }
}
```

The `files` map uses relative paths from the install directory (e.g., `~/.claude/`). Values are empty strings in the template -- the installer populates them with `sha256:HASH` values at install time. Include `schema_version: 1` so future versions can migrate.
  </action>
  <verify>
    <automated>cat design-version.json | python3 -c "import sys,json; d=json.load(sys.stdin); assert len(d['files'])==16; assert d['version']=='1.0.0'; assert d['schema_version']==1; print('OK')"</automated>
  </verify>
  <done>design-version.json exists at repo root with version "1.0.0", schema_version 1, and exactly 16 file entries covering all design-layer files</done>
</task>

<task type="auto">
  <name>Task 2: Create patched update.md command with design-layer preservation</name>
  <files>.claude/commands/gsd/update.md</files>
  <action>
Create a patched `.claude/commands/gsd/update.md` that wraps the vanilla GSD update with design-file backup and restore. This overwrites the vanilla update.md command shim.

The command shim must contain ALL design-preservation logic inline (not in a separate workflow file that would be wiped). Structure:

**Frontmatter:** Same as vanilla (`name: gsd:update`, `allowed-tools: [Bash, AskUserQuestion, Read, Write]`) -- add Read and Write since backup/restore needs file operations.

**Objective:** Same as vanilla but mention design-layer preservation.

**execution_context:** Reference `@{INSTALL_DIR}/get-shit-done/workflows/update.md` (same as vanilla -- the workflow handles version checking and changelog).

**Process section -- 3 phases:**

Phase 1 - BACKUP (before GSD update):
- Detect install dir by checking for `get-shit-done/VERSION` in local dirs first (`.claude`, `.config/opencode`, `.opencode`, `.gemini`), then global (`$HOME/` prefixed).
- Create temp backup dir: `mktemp -d`
- Read `design-version.json` from `{INSTALL_DIR}/get-shit-done/design-version.json` if it exists (preserve metadata).
- Copy ALL 16 design files + design-version.json to temp dir, preserving relative directory structure. Use the explicit file list from the interfaces block above. Also copy update.md itself.
- Verify backup by counting files.

Phase 2 - UPDATE (vanilla GSD):
- Follow the update workflow from `@{INSTALL_DIR}/get-shit-done/workflows/update.md` end-to-end (version check, changelog, user confirmation, npx install).

Phase 3 - RESTORE (after GSD update):
- Create `get-shit-done/workflows/design/` directory if it was wiped.
- Copy ALL backed-up files from temp dir back to their original locations in the install dir.
- Re-copy the patched `update.md` command (this file) from backup -- critical since the vanilla update just overwrote it.
- Regenerate `design-version.json` with fresh checksums of all restored files (using `shasum -a 256` or `sha256sum` fallback). Update `gsd_base_version` from the new `VERSION` file. Update `installed_at` timestamp.
- Verify restoration: check all 16 files exist.
- Clean up temp dir.
- Log: "Design layer preserved through GSD update."

CRITICAL: The process section must include explicit bash code snippets for backup and restore since this is a prompt file that Claude executes. Include the full file list as a shell variable for iteration. Include the checksum computation function inline.

Important: Use `@/Users/jayvanam/.claude/get-shit-done/workflows/update.md` for the execution_context path (matching the pattern used by other patched command shims in this project).
  </action>
  <verify>
    <automated>grep -c "design" .claude/commands/gsd/update.md | xargs test 5 -le && grep -q "GSD-DESIGN" .claude/commands/gsd/update.md && grep -q "backup" .claude/commands/gsd/update.md && grep -q "restore" .claude/commands/gsd/update.md && grep -q "design-version.json" .claude/commands/gsd/update.md && echo "OK"</automated>
  </verify>
  <done>Patched update.md exists with: (1) backup phase listing all 16 design files, (2) vanilla GSD update delegation, (3) restore phase with checksum regeneration, (4) GSD-DESIGN markers, (5) self-restoration of the patched update.md itself</done>
</task>

</tasks>

<verification>
- design-version.json is valid JSON with 16 file entries
- update.md contains backup, update, and restore phases
- update.md references all 16 design-layer files
- update.md includes self-restoration logic (restores the patched update.md itself after wipe)
</verification>

<success_criteria>
- design-version.json template exists with all 16 design file paths and schema_version 1
- Patched update.md contains inline backup/restore logic that preserves design files through a GSD clean install cycle
- update.md includes checksum regeneration for design-version.json after restore
</success_criteria>

<output>
After completion, create `.planning/phases/06-update-safety-installation/06-01-SUMMARY.md`
</output>
