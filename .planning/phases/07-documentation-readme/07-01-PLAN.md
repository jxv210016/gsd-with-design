---
phase: 07-documentation-readme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - LICENSE
autonomous: false
requirements:
  - R7.1
  - R7.2

must_haves:
  truths:
    - "README explains what GSD with Design is and how it differs from vanilla GSD"
    - "README provides a one-liner install command for immediate use"
    - "README documents all 7 commands with descriptions and usage examples"
    - "README contains 4 Mermaid diagrams showing key flows"
    - "README explains how to uninstall and revert to vanilla GSD"
    - "No code files are modified -- only documentation artifacts created"
  artifacts:
    - path: "README.md"
      provides: "Full project documentation"
      min_lines: 150
      contains: "GSD with Design"
    - path: "LICENSE"
      provides: "MIT license file"
      contains: "MIT License"
  key_links:
    - from: "README.md"
      to: "install.sh"
      via: "curl one-liner install command"
      pattern: "curl.*install\\.sh"
    - from: "README.md"
      to: ".claude/commands/gsd/"
      via: "commands reference table"
      pattern: "gsd:design-thinking"
    - from: "README.md"
      to: "workflows/design/"
      via: "file architecture section"
      pattern: "workflows/design"
---

<objective>
Write a comprehensive README.md documenting the full GSD with Design integration, plus a LICENSE file.

Purpose: Give users discovering this repo everything they need to understand, install, use, and uninstall the design layer fork.
Output: README.md (repo root), LICENSE (repo root)
</objective>

<execution_context>
@/Users/jayvanam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jayvanam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-documentation-readme/07-CONTEXT.md
@.planning/phases/07-documentation-readme/07-RESEARCH.md

<!-- Source files to reference for accuracy -->
@.claude/commands/gsd/design-thinking.md
@.claude/commands/gsd/design-ui.md
@.claude/commands/gsd/design-stack.md
@.claude/commands/gsd/new-project.md
@.claude/commands/gsd/discuss-phase.md
@.claude/commands/gsd/plan-phase.md
@.claude/commands/gsd/update.md
@workflows/design/stack-conventions.md
@workflows/design/ui-design.md
@workflows/design/ux-design.md
@workflows/design/motion-design.md
@workflows/design/ui-detection.md
@workflows/design/orchestrate-design.md
@install.sh
@install.ps1
@design-version.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md and LICENSE</name>
  <files>README.md, LICENSE</files>
  <action>
Create README.md at repo root following the section structure from 07-RESEARCH.md. Use a technical, direct tone per user decision.

**Section order and content:**

1. **Hero** -- 2-3 shields.io badges (version from design-version.json, MIT license, GSD compatible). 1-2 sentence description: "A fork of get-shit-done that adds design thinking to every project." Link to upstream GSD repo. Immediately below, inline the main pipeline Mermaid diagram (design thinking pipeline: new-project -> questioning -> design thinking interview -> DESIGN.md -> research and planning -> phase execution).

2. **Quick Start** -- One-liner curl install:
   ```bash
   curl -fsSL https://raw.githubusercontent.com/{owner}/{repo}/main/install.sh | sh
   ```
   Use `{owner}/{repo}` placeholder. Brief note: "The installer verifies GSD is installed, copies design files, and patches commands." Link to install.sh source.

3. **What It Adds** -- Brief paragraph explaining the superset guarantee. GSD with Design adds design thinking and design agents but never changes behavior for non-design projects. If you skip design thinking, everything behaves identically to vanilla GSD. Mention DESIGN.md as the central artifact. Link to upstream GSD for base GSD concepts.

4. **How It Works** -- Four subsections:
   a. **Design Thinking** -- Brief explanation of `/gsd:design-thinking` producing DESIGN.md with 4 sections (Problem Space, Emotional Core, Solution Space, Brand Identity). Mention it runs during `/gsd:new-project` after questioning.
   b. **UI Phase Agents** -- Collapsible `<details>` with Mermaid diagram showing: discuss-phase -> UI detected? -> yes: stack gate -> 3 parallel agents (ui-design, ux-design, motion-design) -> synthesis -> {phase}-UI.md / no: vanilla GSD. Explain auto-detection (keyword threshold), manual overrides (`<!-- ui-phase -->` / `<!-- no-ui -->`), and that non-UI phases skip entirely.
   c. **File Architecture** -- Collapsible `<details>` with Mermaid diagram showing file layout: commands/gsd/ branch (design-thinking, design-ui, design-stack, patched: new-project, discuss-phase, plan-phase, update), workflows/design/ branch (6 workflow files), .planning/ branch (DESIGN.md, STACK.md, {phase}-UI.md).
   d. **Update Safety** -- Collapsible `<details>` with Mermaid diagram showing: /gsd:update -> backup design files -> vanilla GSD wipe-and-replace -> restore design files -> verify checksums. Mention design-version.json tracks file checksums.

5. **Commands** -- Table with 3 columns: Command, Description, Example.
   Read actual command files to verify names and purposes. Include all 7 commands:
   - `/gsd:design-thinking` -- Run design interview to create DESIGN.md -- `/gsd:design-thinking`
   - `/gsd:design-ui` -- View UI/UX/motion standards (read-only) -- `/gsd:design-ui`
   - `/gsd:design-stack` -- View stack conventions (read-only) -- `/gsd:design-stack`
   - `/gsd:new-project` (modified) -- Adds design thinking step after questioning -- `/gsd:new-project`
   - `/gsd:discuss-phase` (modified) -- Adds UI detection and agent spawning -- `/gsd:discuss-phase`
   - `/gsd:plan-phase` (modified) -- Loads DESIGN.md and {phase}-UI.md context -- `/gsd:plan-phase`
   - `/gsd:update` (modified) -- Preserves design layer during GSD updates -- `/gsd:update`

6. **Installation** -- Three subsections:
   a. **macOS / Linux** -- Repeat the curl one-liner. Mention POSIX sh compatibility.
   b. **Windows / PowerShell** -- Show: `powershell -ExecutionPolicy Bypass -File install.ps1` with note about downloading install.ps1 first.
   c. **Manual Install** -- Brief steps: clone repo, copy .claude/commands/gsd/design-*.md, copy workflows/design/, run install script for patching, or manually copy patched command shims.

7. **Uninstall** -- Per research template: list files to remove (3 new commands, workflows/design/ directory, design-version.json) and note that running `/gsd:update` restores vanilla command versions.

8. **Contributing** -- 2-3 sentence inline paragraph welcoming PRs. Mention the project uses GSD for its own development. No separate CONTRIBUTING.md needed for v1.0.

9. **License** -- "MIT" with link to LICENSE file.

**Mermaid diagram rules (from research pitfalls):**
- Always quote node labels with brackets: `["label text"]`
- Use `graph LR` for pipelines, `graph TD` for hierarchies
- No semicolons in labels
- Stick to `graph` diagram type
- Blank line after `<summary>` tag for collapsible sections
- Blank line before `</details>` closing tag

**GitHub features to use:**
- `> [!NOTE]` callout for the superset guarantee in "What It Adds"
- `> [!TIP]` callout for the design-ui quick reference hint in Commands section

**Create LICENSE file** with standard MIT license text. Use "GSD with Design Contributors" as the copyright holder. Year 2026.

**Critical accuracy check:** Before writing, read each command file's first few lines to verify the command name matches what the README will reference. Read design-version.json to get the actual version number for badges.
  </action>
  <verify>
    <automated>test -f README.md && test -f LICENSE && grep -q "GSD with Design" README.md && grep -q "MIT" LICENSE && grep -c '```mermaid' README.md | grep -q '[4-9]' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>README.md exists with hero, quick start, what it adds, how it works (4 subsections with Mermaid diagrams), commands table (7 entries), installation (3 methods), uninstall, contributing, and license sections. LICENSE file exists with MIT text. All file references match actual repo contents. Technical direct tone throughout.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify README renders correctly on GitHub</name>
  <files>README.md</files>
  <action>
Human verifies the rendered README on GitHub. Push to a branch, open in browser, and check all visual elements.

What was built: Complete README.md with Mermaid diagrams, commands reference, install instructions, and MIT LICENSE file.

How to verify:
1. Open README.md in GitHub (push to branch or use `gh repo view --web` then navigate to README)
2. Verify the main pipeline Mermaid diagram renders inline in the hero section
3. Expand each collapsible `&lt;details&gt;` section -- verify all 3 secondary Mermaid diagrams render
4. Check the commands table has all 7 commands with descriptions and examples
5. Verify the curl install one-liner is present in Quick Start
6. Verify the uninstall section lists correct files to remove
7. Confirm technical, direct tone throughout (no conversational or marketing language)
8. Verify no code files were modified -- only README.md and LICENSE created

Resume signal: Type "approved" or describe issues to fix.
  </action>
  <verify>
    <automated>test -f README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>User has reviewed the rendered README on GitHub and approved it, or issues have been addressed.</done>
</task>

</tasks>

<verification>
- README.md exists at repo root with all required sections
- LICENSE file exists at repo root with MIT text
- 4 Mermaid diagrams present (1 inline, 3 in collapsible sections)
- Commands table has 7 entries with descriptions and examples
- No code files modified (git diff should show only README.md and LICENSE as new files)
- All referenced file paths match actual repo structure
</verification>

<success_criteria>
- README.md renders correctly on GitHub with all Mermaid diagrams visible
- A new user can understand what the project does, install it, and find command reference within 2 minutes of reading
- R7.1 satisfied: README documents full integration (flow, agents, commands, install, update safety)
- R7.2 satisfied: No new dependencies added (documentation-only phase, verified by git diff)
</success_criteria>

<output>
After completion, create `.planning/phases/07-documentation-readme/07-01-SUMMARY.md`
</output>
