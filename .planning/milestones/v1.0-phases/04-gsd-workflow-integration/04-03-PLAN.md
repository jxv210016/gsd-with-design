---
phase: 04-gsd-workflow-integration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/plan-phase.md
  - .claude/commands/gsd/plan-phase.md
autonomous: true
requirements: [R4.3, R4.4]

must_haves:
  truths:
    - "plan-phase loads DESIGN.md and {phase}-UI.md when they exist"
    - "Missing DESIGN.md or {phase}-UI.md causes no errors -- gracefully omitted"
    - "Planner receives design files in its files_to_read prompt when available"
    - "Non-UI phases (no UI.md) get zero design context in the planner"
  artifacts:
    - path: "workflows/plan-phase.md"
      provides: "Patched plan-phase workflow with design context loading injection"
      contains: "GSD-DESIGN-START"
    - path: ".claude/commands/gsd/plan-phase.md"
      provides: "Updated command shim (forked copy)"
      contains: "plan-phase"
  key_links:
    - from: "workflows/plan-phase.md"
      to: ".planning/DESIGN.md"
      via: "optional file existence check"
      pattern: "DESIGN\\.md"
    - from: "workflows/plan-phase.md"
      to: "{phase}-UI.md"
      via: "optional file glob in phase directory"
      pattern: "UI\\.md"
---

<objective>
Patch the plan-phase workflow to inject design context loading at two locations: Step 7 (context path extraction) and Step 8 (planner spawn prompt). Update the command shim as a forked copy.

Purpose: Ensure the planner agent receives DESIGN.md and {phase}-UI.md as optional context so plans for UI phases incorporate design guidance. Missing files are gracefully skipped.

Output: Patched `workflows/plan-phase.md` and `.claude/commands/gsd/plan-phase.md`
</objective>

<execution_context>
@/Users/jayvanam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jayvanam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-gsd-workflow-integration/04-RESEARCH.md

Source files to read before patching:
@~/.claude/get-shit-done/workflows/plan-phase.md
@~/.claude/commands/gsd/plan-phase.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject design context loading into plan-phase workflow</name>
  <files>workflows/plan-phase.md</files>
  <action>
1. Copy `~/.claude/get-shit-done/workflows/plan-phase.md` to `workflows/plan-phase.md` in the project repo (forked copy for installer overlay).

2. **Location A -- Step 7 (context path extraction):** Find "## 7. Use Context Paths from INIT" section (around line 252). After the existing bash block that extracts CONTEXT_PATH, RESEARCH_PATH, etc., inject a design context loading block:

```markdown
<!-- GSD-DESIGN-START -->
## Design Context (optional)

Check for design artifacts -- these are optional and gracefully skipped if missing:

```bash
DESIGN_PATH=""
UI_PATH=""

if [ -f ".planning/DESIGN.md" ]; then
  DESIGN_PATH=".planning/DESIGN.md"
fi

UI_PATH=$(ls "${PHASE_DIR}"/*-UI.md 2>/dev/null | head -1)
```

If DESIGN_PATH or UI_PATH are non-empty, they will be included in the planner prompt below. If both are empty, the planner receives no design context (vanilla behavior).
<!-- GSD-DESIGN-END -->
```

Insert this AFTER the existing context path extraction block in Step 7, BEFORE Step 7.5 (Verify Nyquist Artifacts).

3. **Location B -- Step 8 (planner spawn prompt):** Find "## 8. Spawn gsd-planner Agent" section (around line 281). Inside the planner prompt template, find the `<files_to_read>` block that lists state_path, roadmap_path, requirements_path, context_path, research_path, etc. After the last existing file entry (before the closing `</files_to_read>` tag), inject:

```markdown
<!-- GSD-DESIGN-START -->
- {DESIGN_PATH} (Design brief -- brand direction, emotional core, visual identity. Optional, omit if empty.)
- {UI_PATH} (Phase-specific UI/UX/motion design guidance. Optional, omit if empty.)
<!-- GSD-DESIGN-END -->
```

IMPORTANT: The planner prompt assembles `<files_to_read>` dynamically. The DESIGN_PATH and UI_PATH entries should ONLY be included if the variables are non-empty. Add a note in the injection that these lines should be omitted from the actual prompt if the paths are empty strings. Use the same conditional pattern as verification_path and uat_path which already have "if --gaps" conditions.

4. Also add a small note in the planner prompt (after the `<files_to_read>` block) within markers:

```markdown
<!-- GSD-DESIGN-START -->
**Design context:** If DESIGN.md or {phase}-UI.md paths are included above, the planner should reference design constraints when creating UI-related tasks. These files are informational only -- never fail if missing.
<!-- GSD-DESIGN-END -->
```

5. Do NOT modify any other part of the workflow file.
  </action>
  <verify>
    <automated>grep -c "GSD-DESIGN-START" workflows/plan-phase.md && grep -c "GSD-DESIGN-END" workflows/plan-phase.md && grep "DESIGN_PATH" workflows/plan-phase.md | head -1 && grep "UI_PATH" workflows/plan-phase.md | head -1 && grep "DESIGN.md" workflows/plan-phase.md | head -1</automated>
  </verify>
  <done>workflows/plan-phase.md contains 3 GSD-DESIGN-START/END marker pairs: (1) design context loading in Step 7, (2) files_to_read entries in Step 8 planner prompt, (3) design context note in planner prompt. DESIGN_PATH and UI_PATH are conditionally resolved. Missing files produce empty variables that are omitted from the prompt.</done>
</task>

<task type="auto">
  <name>Task 2: Create forked plan-phase command shim</name>
  <files>.claude/commands/gsd/plan-phase.md</files>
  <action>
1. Copy `~/.claude/commands/gsd/plan-phase.md` to `.claude/commands/gsd/plan-phase.md` in the project repo (forked copy for installer overlay).

2. The plan-phase command shim does NOT need additional execution_context references for design files. The plan-phase workflow only checks for DESIGN.md and UI.md file existence using bash commands -- it does not @ reference any design workflow files. The planner agent (gsd-planner) receives these files via files_to_read, not via execution_context.

3. Simply create the forked copy as-is. The workflow file patch (Task 1) handles all the design integration logic.

4. Verify that this plan's own files are correct:
   - `.claude/commands/gsd/plan-phase.md` exists as a valid forked copy
   - `workflows/plan-phase.md` (from Task 1) contains GSD-DESIGN-START markers with conditional bash guards (`if [ -f` pattern) for DESIGN_PATH and UI_PATH

NOTE: Cross-file R4.4 guard clause verification across all three workflow files (new-project.md, discuss-phase.md, plan-phase.md) is handled in the phase-level verification section below, which runs after all plans complete. This task only verifies files owned by this plan.
  </action>
  <verify>
    <automated>test -f .claude/commands/gsd/plan-phase.md && echo "shim exists" && grep -c "GSD-DESIGN-START" workflows/plan-phase.md && grep "if \[" workflows/plan-phase.md | grep -i "design\|DESIGN" | head -2</automated>
  </verify>
  <done>.claude/commands/gsd/plan-phase.md exists as forked copy. workflows/plan-phase.md contains GSD-DESIGN-START markers with conditional file-existence guards for design paths.</done>
</task>

</tasks>

<verification>
All checks pass:
- `grep -c "GSD-DESIGN-START" workflows/plan-phase.md` returns 3
- `grep "DESIGN_PATH" workflows/plan-phase.md` shows conditional path extraction
- `grep "UI_PATH" workflows/plan-phase.md` shows glob-based path detection
- `grep "DESIGN.md" workflows/plan-phase.md` shows existence check
- `.claude/commands/gsd/plan-phase.md` exists as forked copy

Cross-file R4.4 guard clause verification (run after all 3 plans in Phase 04 complete):
```bash
echo "=== Marker injection count ==="
grep -c "GSD-DESIGN-START" workflows/new-project.md workflows/discuss-phase.md workflows/plan-phase.md

echo "=== Guard clause presence ==="
for f in workflows/new-project.md workflows/discuss-phase.md workflows/plan-phase.md; do
  echo "--- $f ---"
  grep -A3 "GSD-DESIGN-START" "$f" | grep -i "design.md\|auto mode\|exists\|if \[" | head -3
done

echo "=== Command shim design refs ==="
grep -l "design" .claude/commands/gsd/new-project.md .claude/commands/gsd/discuss-phase.md .claude/commands/gsd/plan-phase.md
```

This cross-file check depends on outputs from Plans 01 and 02 and is intentionally placed here in the phase-level verification (not in any task) so it runs only after all plans have completed.
</verification>

<success_criteria>
- workflows/plan-phase.md exists as a complete copy with 3 injection blocks
- Step 7 injection: DESIGN_PATH and UI_PATH conditionally resolved via bash
- Step 8 injection: planner prompt includes DESIGN_PATH and UI_PATH in files_to_read (when non-empty)
- Step 8 injection: note explaining design context is informational only
- Missing files produce empty paths that are omitted from planner prompt (R4.3 graceful)
- .claude/commands/gsd/plan-phase.md exists as forked copy
- R4.4 plan-phase.md guard: conditional bash checks for DESIGN.md and UI.md existence
- R4.4 cross-file verification: deferred to phase-level verification section (requires Plans 01 and 02 outputs)
</success_criteria>

<output>
After completion, create `.planning/phases/04-gsd-workflow-integration/04-03-SUMMARY.md`
</output>
