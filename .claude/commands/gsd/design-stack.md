---
name: gsd:design-stack
description: Quick reference for stack conventions and framework recipes
argument-hint: ""
allowed-tools:
  - Read
---

<objective>
This is a read-only reference command. Output ONLY the reference content. Do NOT add project-specific analysis, next-step suggestions, or commentary beyond the reference.

Display stack conventions with framework-specific recipes for the four design dimensions: spacing, color, typography, and motion.
</objective>

<process>

## Step 1: Load project context

Read `.planning/DESIGN.md`.

**If it exists:** Extract Solution Space > Tech Stack (Framework, Styling, Key Libraries).

**If it does not exist:** Note:
> No DESIGN.md found. Run `/gsd:design-thinking` first.

Read `.planning/STACK.md`.

**If it exists:** Extract the four recipe sections (Spacing, Color, Typography, Motion).

**If it does not exist:** Note:
> No STACK.md found. STACK.md is generated by the stack-conventions agent during the first UI phase. Showing the four design dimensions that will be translated.

Read `workflows/design/stack-conventions.md` for dimension definitions.

---

## Step 2: Output the reference card

Output the following sections. Use tables for token mappings, code blocks for syntax examples. Show the pattern once with one example value per recipe.

---

### Detected Stack

| Property       | Value                                     |
| -------------- | ----------------------------------------- |
| Framework      | {from DESIGN.md or "not detected"}        |
| Styling        | {from DESIGN.md or "not detected"}        |
| Key Libraries  | {from DESIGN.md or "not detected"}        |

---

### Spacing Recipes

**If STACK.md exists:** Output its Spacing section (framework-specific syntax for the 8pt grid scale).

**If STACK.md does not exist:** Output generic CSS custom properties:

```css
:root {
  --space-xs:  4px;
  --space-sm:  8px;
  --space-md:  16px;
  --space-lg:  24px;
  --space-xl:  32px;
  --space-2xl: 48px;
  --space-3xl: 64px;
}
```

---

### Color Recipes

**If STACK.md exists:** Output its Color section (framework-specific token syntax).

**If STACK.md does not exist:** Output generic CSS custom properties:

```css
:root {
  --color-primary:    /* brand primary */;
  --color-secondary:  /* brand secondary */;
  --color-accent:     /* interactive / CTA */;
  --color-background: /* page background */;
  --color-surface:    /* card / container background */;
  --color-text:       /* body text */;
  --color-muted:      /* secondary text */;
  --color-border:     /* dividers and borders */;
  --color-error:      /* error states */;
  --color-success:    /* success states */;
}
```

---

### Typography Recipes

**If STACK.md exists:** Output its Typography section (framework-specific font setup).

**If STACK.md does not exist:** Output generic CSS:

```css
:root {
  --font-family-heading: /* heading typeface */;
  --font-family-body:    /* body typeface */;

  /* Type scale (ratio 1.250) */
  --text-xs:   0.64rem;
  --text-sm:   0.8rem;
  --text-base: 1rem;
  --text-lg:   1.25rem;
  --text-xl:   1.563rem;
  --text-2xl:  1.953rem;
  --text-3xl:  2.441rem;
}
```

---

### Motion Recipes

**If STACK.md exists:** Output its Motion section (framework/library-specific animation syntax).

**If STACK.md does not exist:** Output generic CSS:

```css
:root {
  --duration-fast:   150ms;
  --duration-normal: 250ms;
  --duration-slow:   400ms;

  --ease-out: cubic-bezier(0, 0, 0.2, 1);
  --ease-in:  cubic-bezier(0.4, 0, 1, 1);
  --ease-in-out: cubic-bezier(0.4, 0, 0.2, 1);
}

@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}
```

</process>
